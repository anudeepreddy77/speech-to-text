# Core
streamlit>=1.35.0
openai-whisper>=20231117

# Whisper dependencies (often pre-installed but pinned for safety)
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
ffmpeg-python>=0.2.0

# Utilities
more-itertools>=9.0.0
tqdm>=4.65.0
